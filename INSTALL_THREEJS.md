# Installing Three.js Dependencies

The Skill Tree feature requires Three.js packages. To install them, run:

```bash
npm install
```

Or if you need to install just the Three.js packages:

```bash
npm install three @react-three/fiber @react-three/drei
```

After installation, restart your dev server:

```bash
npm run dev
```


